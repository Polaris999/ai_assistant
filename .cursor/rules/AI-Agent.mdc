---
alwaysApply: true
---

AI Agent 项目开发规范 (Cursor Rules)
核心目标：本规则旨在确保项目代码具备清晰的 Agent 架构、高度的可维护性、健壮的逻辑处理，并符合业界开发 AI Agent 的最佳实践。
1. 项目结构与架构
分层明确：必须严格区分控制层（Orchestrator/Agent）、工具层（Tools）、记忆层（Memory）、知识层（Retrieval）和基础设施层（LLM Client, Vector DB）。禁止在单个文件中混合不同层的逻辑。
模块化：每个工具（Tool）、链（Chain）或关键能力应封装在独立的模块或类中。agent.py或 main.py应主要负责工作流的编排，而非具体实现。
配置分离：所有模型参数（如 temperature, max_tokens）、API 密钥、系统提示词（System Prompt）必须从代码中分离，使用配置文件（如 config.yaml、.env）或配置类进行管理。
2. 工具（Tools）定义与使用
规范定义：每个工具应是一个有明确单一职责的函数或类。工具函数必须包含清晰的文档字符串（Docstring），说明其功能、输入参数（Schema）和返回值。
错误处理：工具内部必须实现详尽的错误处理（try-catch），并将异常转换为对 Agent 友好的信息或特定错误类型向上传递，避免因工具崩溃导致 Agent 主循环中止。
依赖注入：工具所依赖的外部服务客户端（如数据库、API）应通过构造函数或参数注入，而非在工具内部硬编码创建，以方便测试和替换。
3. 提示词（Prompts）管理
模板化：禁止在代码逻辑中硬编码长篇提示词。所有提示词应定义为模板字符串，并集中存放在 prompts/目录下的模块或 .jinja2文件中。
变量清晰：提示词模板中的变量占位符应具有业务含义（如 {user_query}, {context}），避免使用模糊的 {text}。
版本控制：对提示词的重要修改需在注释或文档中说明原因，以便追踪迭代效果。
4. 记忆（Memory）与状态管理
接口抽象：记忆操作（保存、读取、搜索）应通过统一的接口（如 Memory类）进行，而非直接调用向量数据库或缓存的原始客户端。
上下文管理：对话历史或短期记忆的管理应自动处理 token 计数与摘要，防止上下文窗口溢出。
记忆结构：存入长期记忆的内容应具有清晰的结构（如包含时间戳、会话ID、源信息等元数据）。
5. 核心逻辑与健壮性
主循环健壮：Agent 的“思考-执行-观察”循环必须包含全面的异常捕获、超时控制和最大重试次数限制。
流式支持：如果涉及流式响应，应正确处理流式数据，并确保在中断时能优雅地释放资源。
验证与解析：对 LLM 的输出（如 JSON、特定指令）必须进行严格的格式验证和解析异常处理，不能假设 LLM 总是返回完美结果。
6. 代码质量与可维护性
日志记录：关键决策点（如工具选择、最终答案生成）、错误和警告必须通过结构化日志记录，日志应包含请求ID、会话ID等用于追踪的字段。
注释原则：注释应解释“为什么这么做”和复杂算法的意图，而非描述“代码在做什么”。及时删除已注释的废弃代码。
配置化开关：用于功能降级、切换模型或启用调试模式的开关，应通过配置控制，而非修改代码。
7. 文档
项目README：必须包含架构框图、环境设置命令、配置说明、以及一个最简单的端到端运行示例。
工具文档：每个重要工具或模块应有对应的使用示例，说明其输入和输出格式。
规则执行：在生成新代码、重构或审查代码时，请依据以上规则进行评估，并对不符合项提供具体的修改建议。
